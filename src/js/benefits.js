// const featureGrid = document.querySelector('.featuresGrid');
// const features = Array.from(document.querySelectorAll('.feature'));

// const numberOfClones = 8;

// for (let i = 0; i < numberOfClones; i++) {
//   features.forEach(feature => {
//     const clone = feature.cloneNode(true);
//     featureGrid.appendChild(clone);
//   });
// }
