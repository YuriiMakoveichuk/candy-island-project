// const slider = document.querySelector('.benefits-grid');
// const slides = document.querySelectorAll('.benefits-item');
// console.log(slides);

// let currentPosition = 0;

// function moveSlides() {
//   currentPosition -= 1;
//   slider.style.transform = `translateX(${currentPosition}%)`;

//   if (currentPosition <= 50) {
//     currentPosition = 1;
//   }

//   requestAnimationFrame(moveSlides);
// }

// requestAnimationFrame(moveSlides);
